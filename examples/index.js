import Vue from 'vue'
import App from './App.vue'

// import Player from '../lib/vue-vplayer.umd'
// Vue.use(Player)

// eslint-disable-next-line no-new
new Vue({
  el: '#app',
  render: h => h(App)
})
